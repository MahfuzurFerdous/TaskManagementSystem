@model EditTaskCardViewModel

<form id="editTaskForm" asp-action="Edit" asp-controller="TaskCard" method="post" novalidate>
    <input type="hidden" asp-for="Id" />

    <div class="modal-body">
        <div class="form-group">
            <label asp-for="Title" class="control-label"></label>
            <input asp-for="Title" class="form-control" />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Description" class="control-label"></label>
            <textarea asp-for="Description" class="form-control" rows="3"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="AssignedToUserName" class="control-label">
                <i class="fas fa-flag text-primary mr-1"></i> Assigned To User
            </label>
            <select asp-for="AssignedToUserName" asp-items="Model.AvailableUsers" class="form-control select2bs4"></select>
        </div>

        @* <div class="form-group"> *@
        @*     <label asp-for="Status" class="control-label"> *@
        @*         <i class="fas fa-flag text-primary mr-1"></i> Status *@
        @*     </label> *@
        @*     <select asp-for="Status" asp-items="Model.StatusList" class="form-control select2bs4"></select> *@
        @* </div> *@

        <div class="form-group">
            <label asp-for="DueDate" class="control-label"></label>
            <div class="input-group">
                <div class="input-group-prepend">
                    <span class="input-group-text"><i class="far fa-calendar-alt"></i></span>
                </div>
                <input asp-for="DueDate" type="date" class="form-control" />
            </div>
            <span asp-validation-for="DueDate" class="text-danger"></span>
        </div>
    </div>

    <div class="modal-footer justify-content-between">
        <button type="submit" class="btn btn-primary">
            <i class="fas fa-save"></i> Save
        </button>
    </div>
</form>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")

    <script>
        $(function () {
            $('.select2bs4').select2({
                theme: 'bootstrap4',
                dropdownParent: $('#editTaskForm').closest('.modal')
            });
        });

    </script>
}
